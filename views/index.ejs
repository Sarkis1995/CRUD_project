<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>CRUD Project</h1>
    <form action="/quotes" method="POST">
        <input type="text" placeholder="name" name="name">
        <input type="text" placeholder="quote" name="quote">
        <button type="submit">Submit</button>
    </form>
    <h2> Quotes </h2>
    <ul class="quotes">
        <% for (let i=0; i < quotes.length; i++) {%>
            <li class="quotes">
                <span><%= quotes[i].name %></span>:
                <span><%= quotes[i].quote %></span>
            </li>
        <% } %>
    </ul>
    <div>
        <h2> Quote replacement!</h2>
        <p>
            Replace first quote with a different quote
        </p>
        <button id="update-button">Replace quote</button>
    </div>
    <div>
        <h2>Remove Quote</h2>
        <p>Delete one quote, does nothing if there are no more quotes</p>
        <button id="delete-button">Delete quote</button>
    </div>
    <div id="message"></div>
    <script src="/main.js"></script>
</body>
</html>